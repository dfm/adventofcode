use aoc::run;

fn part1(_input: Vec<usize>) -> usize {
    0
}

fn part2(_input: Vec<usize>) -> usize {
    0
}

fn main() {
    run({{year}}, {{day}}, part1).unwrap();
    run({{year}}, {{day}}, part2).unwrap();
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_part1() {
        assert_eq!(part1(vec![]), 0);
    }

    #[test]
    fn test_part2() {
        assert_eq!(part2(vec![]), 0);
    }
}
