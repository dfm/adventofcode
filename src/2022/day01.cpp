#include <iostream>

#include "aoc/aoc.hpp"

int part1(const std::string &in) { return 0; }

unsigned int part2(const std::vector<int> &in) { return 2; }

int main() {
  // aoc::register_implementation(2022, 10, part1, part2);
  // aoc::register_implementation(2021, 5, part1, part2);
  // aoc::register_implementation(2022, 1, part1, part2);

  // auto registry = aoc::registry();
  // for (const auto &el : registry) {
  //   std::cout << el.first.first << " " << el.first.second << std::endl;
  // }
  return 0;
}